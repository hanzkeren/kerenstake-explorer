{
  "version": 2,
  "installCommand": "yarn install --frozen-lockfile || yarn install",
  "buildCommand": "yarn build",
  "env": {
    "PUBLIC_PATH": "/"
  },
  
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
